spring:
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///hm49?characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: 123456
  # jpa配置
  jpa:
    database: MySQL
    show-sql: false